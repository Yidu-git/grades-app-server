import express from "express";
// import { fetchNotes } from "../Controllers/NoteController.js"; // Corrected import path

const router = express.Router();

// router.get("/", fetchNotes);

export default router;
